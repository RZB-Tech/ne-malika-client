"use client";

import { useChatStream } from "@/lib/api/chat-stream";

/**
 * Держит живой канал переписки открытым, пока открыта вкладка.
 *
 * Отдельным компонентом в корне, а не внутри раздела сообщений: значок с числом
 * непрочитанного висит в шапке на каждой странице, и обновляться он должен там
 * же, а не только когда чат открыт. Ничего не рисует.
 */
export function ChatStream() {
  useChatStream();
  return null;
}
